# План доработок «Окулус Бизнес» — цикл 2 (волна 4)

Тот же режим, что и цикл 1: стадии → деплой → агент-контролёр → дожим до ЗАЧТЕНА.
DoD: работает на проде, typecheck/тесты зелёные, видно в UI.

## Стадия 4А — быстрые победы

- [x] 4.1 **Дельты к прошлому месяцу**: в financeSummaryResponseSchema добавлены
  prevMonthIncome/prevMonthExpense (соседний месяц из Txn); на тайлах «Доход»/«Расход»
  страницы Финансы — подпись «+X% к прошлому месяцу» (или «новый месяц», если прошлого нет).
  DoD: на проде у тайлов виден процент изменения.
- [x] 4.2 **Колокольчик уведомлений**: users.lastSeenNotificationsAt (миграция);
  GET /api/notifications (просроченные действия + комментарии сделок новее lastSeen,
  максимум 20, с counters) и POST /api/notifications/seen. В AppShell — колокольчик с бейджем
  (шапка телефона + низ сайдбара ПК), дропдаун со списком и кнопкой «Прочитано».
  DoD: бейдж растёт от новой просрочки/комментария, «Прочитано» сбрасывает.
- [x] 4.3 **Отчёт владельца (год к дате)**: на странице Прогноз внизу таблица 12 месяцев
  из /api/finance/history: выручка, расход, прибыль, маржа % + строка «Итого 12 мес»
  (суммы и средняя маржа). DoD: таблица с маржой и итогами на проде.

## Стадия 4Б — контрагенты (крупная)

- [x] 4.4 **Контрагенты как сущность**: модель Company {id, name, note}; Deal.companyId
  (миграция + бэкфилл: companies из уникальных названий сделок, deals.company_id проставлен);
  API GET/POST /api/crm/companies; в dealSchema companyId/companyName, выбор в карточке сделки
  (селект + «создать нового» по вводу имени); экран «Контрагенты» (роут /app/companies,
  пункт в сайдбаре): список с числом сделок и активным MRR, раскрытие → список сделок;
  контрагенты ищутся в Ctrl+K. DoD: справочник открывается, сделка привязывается, бэкфилл виден.
- [x] 4.5 **Номер счёта у дебиторки**: ExpectedPayment.invoiceNumber (миграция), поле
  «№ счёта» в форме ожидаемого поступления и в списке. DoD: номер сохраняется и виден.

## Стадия 4В — полировка

- [x] 4.6 **Спарклайны на KPI**: на дашборде под плитками MRR и «Доход/расход месяца»
  мини-бары последних 6 месяцев из /api/finance/history. DoD: мини-графики видны.
- [x] 4.7 **Экспорт доски сделок в CSV**: кнопка на доске CRM — компании, этап, суммы,
  следующее действие, причина отказа, контрагент. DoD: файл скачивается и открывается.

## Отложено (осознанно)

Полноценный ОПиУ по начислению (требует другого учёта) · email · общий audit-log ·
интеграция с банками/1С.

## Сводка статуса (ведёт контролёр)

- Стадия 4А: завершена 2026-09-12 (деплой + проверки)
- Стадия 4Б: завершена 2026-09-12
- Стадия 4В: завершена 2026-09-12
